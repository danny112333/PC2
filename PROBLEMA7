def is_perfect(num):
    divisors = [i for i in range(1, num) if num % i == 0]
    return sum(divisors) == num

def problem_7():
    perfect_numbers = [num for num in range(1, 1000) if is_perfect(num)]
    return perfect_numbers
print("Problem 7:", problem_7())
